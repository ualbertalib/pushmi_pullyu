# Dummy sample file for testing parsing of configuration file into PushmiPullyu

debug: true
logdir: tmp/spec/log
monitor: false
piddir: tmp/spec/pids
workdir: tmp/spec/work
process_name: test_pushmi_pullyu
minimum_age: 1
queue_name: test:pmpy_queue
rollbar:
  token: 'abc123xyz'
  proxy_host: 'your_proxy_url'
  proxy_port: '80'

solr:
  url: http://www.example.com:8983/solr/dev

fedora:
  url: http://www.example.com:8080/fcrepo/rest
  base_path: '/dev'

swift:
  auth_url: http://example.com:8080/auth/v1.0
